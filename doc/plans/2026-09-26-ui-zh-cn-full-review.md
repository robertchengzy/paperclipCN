# 全量中文词面对照与显示文案修订（2026-09-26）

基线：`d0942b3a8c718441029ac88c8700aebbc5a3431d`。开发分支：`codex/zh-semantic-review-20260926`。后续发布提交与部署事实以实例文档 `DEPLOYMENT.md` §三十五为准。

## 结果

对基线 14,797 条英中词条完成逐条 AI 词面对照，修改 256 条既有中文，新增 31 个显示键；候选中英文各 14,828 条。原有英文值全部保持不变，代码标识、用户内容和原始诊断保留。

重点修正授权码与代码、SSH 认证代理与智能体、任务等待与智能体空闲、未完成任务与待办、归档记录与磁盘删除、共享凭据与独占凭据、单个 Token 与整个网关等含义。旧 `canCreateSkills` 字段明确标为兼容设置，不再将其描述成当前技能授权门；未修改权限实现。

已完成的显示修复保留：动态状态仅在中文显示映射，未知值保留；预算暂停使用专用提示；技能拒绝按实际类型显示；语言切换重新生成缓存文案；中文时间使用 24 小时制；技能标识与认证密钥、聘用职位与策略标题分别取词。

## 校对数据

实例文档仓库 `/personal/paperclip/localization-audit/2026-09-26-full-review/` 保存 `review.csv`、`changes.csv`、`added-keys.csv`、`decisions.csv`、`context-gaps.csv`、基线词表与校验记录。`summary.md` 是交付入口。

词面结果为 256 条修改、14,488 条保留、53 条上下文待处理结论。源码证据分别为 2,233 条阅读过所列上下文、12,473 条仅核对静态引用行、91 条消费链未定位。两类统计独立；AI 对照不等于人工或浏览器验收。没有必须交给用户选择的译法，未定位项保留工程查证状态。

## 验证与范围

用户最后要求停止追加 UI 测试和验收，因此只完成词表键、占位符、现有标记/安全检查、候选数据与差异核对。没有将全 UI 测试、构建、浏览器或真实设备标记为本次候选已通过。

此前已经完成的专项测试和测试代码保留，证据按恢复前历史工具结果及恢复后实际日志区分。执行环境切换后旧 `/tmp` 工作副本和部分日志不可读；审校台账来自已持久保存的 CSV，源码按已记录的实现恢复到本开发分支。没有改动现网实例或运行时 release。

## 后续交付授权（2026-09-26）

用户随后要求合并 fork 更新、推送并部署。重新 fetch 后 `origin/master` 仍为上述基线，没有额外提交需要合并；本批按部署流程进行 UI 类型、词表与设计 token 检查，并从冻结 Git 对象完整构建。此前“不追加 UI 测试和浏览器验收”的要求继续保留，部署健康检查独立记录。构建通过后线性合回 `master` 并普通推送，不对运行时 release 修改代码或拉取更新。
