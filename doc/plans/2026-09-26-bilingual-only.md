# 中英文范围收敛与侧栏语言切换（2026-09-26）

本次从 `04fc50b6048c6223a729d1c69c8a97031b973827` 继续，按用户要求仅保留简体中文和英文。英文兼容基线是上一批之前的 `acbaa3eddb71cb55fdadb4c6539f08bcf5a952c4`。

## 撤回与保留

- 恢复上一批改写的 18 条英文：重试 11、Codex 2、预算 2、密钥定义删除 1、重置 1、周计划 1。发现的英文原文问题只记录，不借中文精校改变英文界面。
- 两类活动映射的英文恢复原回退显示 `provider trace metadata listed` / `instance settings experimental updated`，中文映射保留。
- 删除 38 份其它语言镜像（包括 zh-TW）；同步脚本仅维护 en/zh-CN，并拒绝额外语言文件。运行时原本就只打包这两种语言，本次同时收敛源码目录。
- 保留 164 条中文修订和 13 个既有新增显示键；切换按钮增加 4 个中英文键。保留时间分类和状态显示修复、手机保存栏/公告避让及已有测试修复，不改业务 API、权限、调度、保存/取消处理或数据库。

## 入口与兼容

两套侧栏把左下角反馈外链换为语言按钮：中文时显示 EN，英文时显示中；Tooltip/无障碍名称说明目标语言。复用 setUiLanguage 和 paperclip.ui.language，默认中文，偏好随刷新保留。按钮 type=button，不提交表单、不刷新页面、不导航到外部。

展开侧栏和手机抽屉在原位置显示按钮；折叠侧栏通过账户菜单选择语言。两套账户菜单均提供选择器，登录页入口保留。云模式不隐藏语言按钮，原账户、主题与退出流程不变。

## 验证与交付

验证中英文词表结构、插值和既有英文与 acbaa3edd 一致；专项覆盖双向切换、存储不可用、未保存表单保留、折叠菜单与云/本地退出。浏览器回归覆盖两布局、320/390/430px 与桌面、Enter/Space 切换、刷新保持、真实隔离保存、导航与公告行为。按用户对验证范围的进一步要求，执行 UI 类型检查、国际化与 token gates、相关专项测试和浏览器回归，冻结构建后再发布；已启动的全 UI 套件停止并记录未完成，不作为通过证据。实际测试、源码 SHA 和部署结果由实例文档新章节记录。

物理 iPhone/WebKit 与真实软键盘不计为已验收；不运行与本批 UI 修改无关的后端全量测试。此前审核/部署记录保留历史，当前台账另记英文恢复和语言精简，不覆盖旧证据。

### 本地验证结果

最终相关单测 11 个文件、77 项通过；UI typecheck、locales:check、token gates 通过。浏览器单独运行 10/10 通过，包含键盘双向切换、未保存表单保留、刷新、隔离保存及公告/移动导航回归。并行首跑 2 个浏览器整例超时后主动中止，未放宽超时，串行复测通过；全 UI 套件按用户后续要求中止，未宣称通过。

静态对比：acbaa3edd 的 14,780 条既有英文完全一致；04fc50b60 的 14,793 条中文原值全部保留。本批仅增加切换按钮 4 键，当前中英文各 14,797 键；上一批布局与重试分类源码字节未改。
